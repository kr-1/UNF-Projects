This assignment reads a list of information regarding vehicles and their owners from a file, test.txt, and saves them as an array of
vehicle objects. The program then uses different methods to sort the vehicle objects by vehicle type, owner name, email, or area code.
